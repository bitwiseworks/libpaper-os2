#! /bin/sh -e

#DEBHELPER#

case "$1" in
  purge)
    if [ "${DPKG_MAINTSCRIPT_PACKAGE_REFCOUNT:-1}" = 1 ]; then
      [ -x /usr/bin/ucf ] && ucf --purge /etc/papersize
      rm -f /etc/papersize
    fi
  ;;
esac
exit 0

